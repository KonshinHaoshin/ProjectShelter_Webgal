name: Deploy WebGAL to COS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare deploy bundle
        run: |
          mkdir -p .deploy
          rsync -av --delete \
            --exclude '.git/' \
            --exclude '.github/' \
            --exclude '.claude/' \
            --exclude '.deploy/' \
            --exclude 'webgal_resource_analyzer/' \
            ./ .deploy/

      - name: Upload to COS and Refresh CDN
        uses: sylingd/tencent-cos-and-cdn-action@v1.5.1
        with:
          secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
          cos_bucket: shelter-1386441538
          cos_region: ap-chengdu
          local_path: .deploy
          remote_path: webgal
          cdn_prefix: https://webgal.shelter.net.cn/webgal/
          clean: true
